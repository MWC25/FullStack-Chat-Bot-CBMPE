export default function Notification({ children }: { children?: React.ReactNode }) {
    return (
        <div className="flex flex-row w-full h-full">
            <h1 className="title-1">Notification</h1>
        </div>
    );
}
